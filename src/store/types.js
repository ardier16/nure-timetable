const mutations = {
  // i18n
  SET_LOCALE: 'SET_LOCALE',
}

const actions = {
  // i18n
  CHANGE_LOCALE: 'CHANGE_LOCALE',
}

const getters = {
  // i18n
  locale: 'locale',
}

export const types = {
  ...mutations,
  ...actions,
  ...getters,
}
