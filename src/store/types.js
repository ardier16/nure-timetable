const mutations = {
  SET_LOCALE: 'SET_LOCALE',
}

const actions = {
  CHANGE_LOCALE: 'CHANGE_LOCALE',
}

const getters = {
  locale: 'locale',
}

export const types = {
  ...mutations,
  ...actions,
  ...getters,
}
